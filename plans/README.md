# Implementation Plans

Generated by the improve skill (`/improve next` — direction-only audit) on 2026-07-07, at commit `0e964c2`. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status | Issue |
|------|-------|----------|--------|------------|--------|-------|
| 001  | Expose fight results (winners) in the lookupEvents tool | P1 | S | — | TODO | [#9](https://github.com/tmatti/gu/issues/9) |
| 002  | Scheduled fight-card previews and results recaps (Cron Triggers) | P2 | M | 001 | TODO | [#10](https://github.com/tmatti/gu/issues/10) |
| 003  | Spike: automate the fighter KV cache refresh (report, not code) | P2 | M | — | TODO | [#11](https://github.com/tmatti/gu/issues/11) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 002 requires 001: the Sunday recap consumes the `winner`/`completed`/`status` fields that 001 adds to `mapScoreboardEvents`.
- 002 and 003 both eventually touch `triggers.crons` in `wrangler.jsonc` (003 only in its follow-up implementation plan). Whichever lands second merges cron expressions into the existing array and extends the `controller.cron` dispatch.
- 003 is a spike — its deliverable is `plans/003-spike-report.md`; the actual refresh implementation gets its own plan afterward.

## Findings considered and rejected (or deferred by the maintainer)

Direction findings presented on 2026-07-07 and not selected for planning — recorded so they aren't re-audited from scratch:

- **DIR-04 Multi-promotion promise**: system prompt claims Bellator/ONE knowledge (`src/agent.ts:13`) but all structured data is UFC-only (`src/espn.ts:1-3`, `src/tools/rankings.ts:6-20`). Not selected this round; the cheap half (scoping the prompt to match reality) remains an S-effort option.
- **DIR-05 Progressive status updates**: static "thinking..." fronts up to 8 tool steps (`src/agent.ts:96`); the tool-logging wrapper (`src/tools/index.ts:12`) is the natural hook. Not selected this round.
- **DIR-06 Persistent conversation memory**: DM context capped at 20 msgs / 1 hour (`src/slack.ts:91-92`); KV infra already available. Not selected this round.

Note: this run audited only the **direction** category. Correctness, security, performance, tests, tech debt, dependencies, DX, and docs were not audited — a `standard` or `deep` run would cover them. (One observation in passing: the test suite covers only `/health`, so plans 001–002 each scaffold their own tests.)
